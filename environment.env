# Django Settings
SECRET_KEY=django-insecure-key-change-in-production-please-use-a-secure-key
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,your-domain.com

# Database Configuration (SQLite)
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=/app/data/db.sqlite3

# GitLab API Configuration
GITLAB_API_URL=https://git.economia.gov.br/
GITLAB_SSL_VERIFY=False
GITLAB_TOKEN=cole-seu-token-aqui

# Security Settings
SECURE_SSL_REDIRECT=False
SECURE_HSTS_SECONDS=0
SECURE_HSTS_INCLUDE_SUBDOMAINS=False
SECURE_HSTS_PRELOAD=False
SECURE_CONTENT_TYPE_NOSNIFF=True
SECURE_BROWSER_XSS_FILTER=True
X_FRAME_OPTIONS=DENY

# CORS Settings
CORS_ALLOW_ALL_ORIGINS=False
CORS_ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000,https://your-domain.com

# Cache Settings
CACHE_BACKEND=django.core.cache.backends.locmem.LocMemCache
CACHE_LOCATION=gitlab-metrics-cache
CACHE_TIMEOUT=3600
CACHE_MAX_ENTRIES=1000

# Session Settings
SESSION_ENGINE=django.contrib.sessions.backends.cached_db
SESSION_COOKIE_AGE=86400
SESSION_COOKIE_SECURE=False
SESSION_COOKIE_HTTPONLY=True
SESSION_COOKIE_SAMESITE=Lax

# Static Files
STATIC_URL=/static/
STATIC_ROOT=/app/staticfiles
STATICFILES_STORAGE=whitenoise.storage.CompressedManifestStaticFilesStorage

# Logging
LOG_LEVEL=WARNING
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# Gunicorn Settings
GUNICORN_WORKERS=3
GUNICORN_TIMEOUT=120
GUNICORN_BIND=0.0.0.0:8000

# Application Settings
LANGUAGE_CODE=pt-br
TIME_ZONE=America/Sao_Paulo
USE_I18N=True
USE_TZ=True

# REST Framework
REST_FRAMEWORK_PAGE_SIZE=20
REST_FRAMEWORK_DEFAULT_AUTHENTICATION=rest_framework.authentication.SessionAuthentication
REST_FRAMEWORK_DEFAULT_PERMISSION=rest_framework.permissions.IsAuthenticated
